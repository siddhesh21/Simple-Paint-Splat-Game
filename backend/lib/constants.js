const Constants = {
    MAX_PLAYERS_PER_GAME: 5,
    MAXIMUM_LOBBY_WAIT_TIME: 10, // Seconds
    DEFAULT_GAME_TIME: 60, // Seconds
    DEFAULT_COLORS: ["#1abc9c", "#3498db", "#9b59b6", "#f1c40f", "#e74c3c"],
    SCORE_ON_SPLAT: 1, // Value by which Player's score is increased on new splat
    AI_DELAY: 800, // Delay (in ms) between each AI try
};

module.exports = Constants;